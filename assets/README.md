# Assets Capacitor

Ficheiros gerados por `npm run generate:capacitor-assets` (a partir de `KFS Logo.png` ou `public/icons/icon-512.png`).

Não editar manualmente; voltar a correr o script após mudar o logo.
